@page "/cabinet"
@attribute [Authorize]

<PageTitle>Мой кабинет</PageTitle>
<AuthorizeView>
    <Authorized>
        <UserDisplay Login="@(@context.User.Identity?.Name)" ReadOnly="@(!context.User.IsInRole("Administrator"))"/>
    </Authorized>
</AuthorizeView>